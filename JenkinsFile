pipeline {
    agent any

    environment {
        ARM_CLIENT_ID = credentials('046b6aad-8d6f-480e-9fbc-6b9fdbb9b869')
        ARM_CLIENT_SECRET = credentials('ulo8Q~QhM8XZyDFDzoRxVgAtoHXCMMYim2gZ6ccy')
        ARM_TENANT_ID = credentials('7ee12db0-ba7e-40c4-8543-2c3c0e8ae7d6')
        ARM_SUBSCRIPTION_ID = credentials('ec49dde3-01ff-476f-992d-5460ebb26e8d')
    }
    
    stages {
        stage('Checkout') {
            steps {
                script {
                    // Clone the Git repository's master branch
                    def gitRepoUrl = 'https://github.com/adjune9811/SimpleRG-Jenkins-yaml.git'

                    checkout([$class: 'GitSCM', 
                        branches: [[name: '*/main']], 
                        userRemoteConfigs: [[url: gitRepoUrl]], 
                        extensions: [[$class: 'CleanBeforeCheckout'], [$class: 'CloneOption', noTags: false, shallow: true, depth: 1]]
                    ])
                }
            }
        }

        stage('Init') {
            steps {
                script {
                    // Login to Azure using service principal
                    sh '''
                        az login --service-principal -u $ARM_CLIENT_ID -p $ARM_CLIENT_SECRET --tenant $ARM_TENANT_ID
                        az account set --subscription $ARM_SUBSCRIPTION_ID
                    '''
                    // Initialize Terraform
                    sh 'terraform init'
                }
            }
        }

        stage('Plan') {
            steps {
                script {
                    // Run Terraform plan
                    sh 'terraform plan'
                }
            }
        }

        stage('Apply') {
            steps {
                script {
                    // Apply Terraform changes
                    sh 'terraform apply --auto-approve'
                }
            }
        }
    }

    post {
        always {
            echo 'Prints whether deploy happened or not, success or failure'
        }
    }
}
